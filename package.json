{
  "name": "vanventure-redaktion",
  "private": true,
  "type": "module",
  "scripts": {
    "build:seo": "node tools/build-seo.mjs",
    "start": "docker compose -f compose.yaml -f compose.dev.yaml up --build -d",
    "test": "node --test editor/*.test.mjs deploy/*.test.mjs",
    "check:release": "node deploy/release-check.mjs",
    "check:plans": "node deploy/plan-consistency.mjs",
    "monitor:cockpit": "node deploy/cockpit-monitor.mjs"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "@electric-sql/pglite-socket": "^0.2.11",
    "yaml": "^2.9.1"
  }
}
