{
  "name": "vanventure-redaktion",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "docker compose up --build -d",
    "test": "node --test editor/*.test.mjs"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "@electric-sql/pglite-socket": "^0.2.11",
    "yaml": "^2.9.1"
  }
}
