# PKG-017 – semantische Prüfung SRC-0490 bis SRC-0499

Stand: 25.09.2026. Geprüft: vollständige Originalblöcke `source-inventory.csv:491–500` / `docs/ausbauplan.md:756–784`, Kandidaten `atomic-requirements.csv:625–641`, Zuordnungen `traceability-matrix.csv:491–500`, konkrete Stories/AC `scrum-plan-draft.md:407–455,563–603`, `story-tasks.csv:104–113`, relevante Anker des `constraint-register.md`, `release-decisions.md` und Scrum-Planungsregel. Keine Produktentwicklung, kein Deployment. Originalquellen bleiben bindend.

## Ergebnis und Zählung

10 Quellblöcke ergeben 17 atomare Kandidaten: SRC-0490 **2**, 0491 **4**, 0492 **1**, 0493 **1**, 0494 **2**, 0495 **1**, 0496 **1**, 0497 **2**, 0498 **2**, 0499 **1**. Alle 17 Kandidaten stehen auf `Unresolved`; die zehn Matrixzeilen stehen pauschal auf `Covered`. Das ist nur Blockzuordnung, keine nachgewiesene Klauseldeckung. Ein eigener `constraint-register.md`-Anker für SRC-0490–0499 fehlt; relevante Regeln aus anderen Quellen sind teilweise vorhanden. `TASK-0103–0112` bewahren den Originalwortlaut, ersetzen aber keine nachprüfbaren Story-AC.

## Befunde und konkrete Änderungen

| Quelle und Original | Kandidat/Ziel | Befund und Änderungsvorschlag |
| --- | --- | --- |
| **SRC-0490**, `docs/ausbauplan.md:756–758`, `source-inventory.csv:491` | `.a/.b`, `atomic-requirements.csv:625–626`; Matrix `:491` → `ST-BRD-04`, `scrum-plan-draft.md:595–603`; Task `story-tasks.csv:104` | Trennung von Content Planner und Board ist in `ST-BRD-01:571` sowie `constraint-register.md:3069–3071` allgemein enthalten; `ST-BRD-04:601` behandelt aber Marvin-Autonomie und nennt weder freiwillige Verknüpfung noch **Verbot des automatischen Überschreibens von Status, Inhalt oder Stunden**. `.a` als Board-Grundregel zu `ST-BRD-01`, `.b` als eigenständiges AC für optionale Links mit Test „Änderung einer Seite verändert keine Felder der anderen“ zuordnen. `ST-BRD-04` nur behalten, wenn Marvin diese Verknüpfung tatsächlich bearbeitet; sonst Mapping korrigieren. |
| **SRC-0491**, `:759–763`, Inventory `:492` | `.a–.d`, Atomic `:627–630`; Matrix `:492` → `ST-BRD-04`; Task `:105` | Acht unterschiedliche Prüfbereiche sind in `.a` zu einem Kandidaten gebündelt: Migration, Rechte, CSRF, Transaktionen, Idempotenz, Archiv, Tablet und Restore **von Board, Inbox und Verlauf**. `ST-BRD-04:601` enthält davon nur allgemeine Auditierbarkeit. `ST-BRD-01/02/03` decken Teilfunktionen, aber kein vollständiges Test-/Restore-Gate. `.b` verlangt geschützten Dump **vor Live-Migration**, `.c` danach `/healthz`, private Route und sichtbare Bedienung live, `.d` beschränkt Neustart auf Webdienst bei Bedarf. Pro Prüfdomäne eigene überprüfbare Kriterien/Tasks unter fachlichen Stories oder eine klar abhängige Board-Release-Enabler-Story erstellen. Restore aller drei Datenbereiche ausdrücklich testen; Dump, exakten lokalen Commit/Push, Freigabe des konkreten Release-Umfangs, Migration und Live-Prüfung als getrennte Gates ausweisen. Kein automatisches Deployment aus bestandenem Test. Webdienst allein darf bei Bedarf neu starten; PostgreSQL und andere Dienste bleiben laufen. |
| **SRC-0492**, `:767–768`, Inventory `:493` | `.a`, Atomic `:631`; Matrix `:493` → `ST-INS-02:407–415`; Task `:106` | Inhalt und Ziel passen. Der Kandidat enthält aber zwei getrennt prüfbare Bedingungen: künstliche Grenze im **paginierten** Import entfernen und mit **mehr als 500** Videos automatisiert prüfen. AC `:413` nennt den über-500-Test, aber weder Paginierung/Ende noch den unveränderten Bestand bei Wiederholung. Mindestens Grenzfall 500/501 und Abschluss aller Seiten als Testnachweis konkretisieren; keine bloße Erhöhung der Grenze als Erfüllung werten. |
| **SRC-0493**, `:769–771`, Inventory `:494` | `.a`, Atomic `:632`; Matrix `:494` → `ST-INS-03:417–425`; Task `:107` | Die Administrator-Aktion ist als Story erkennbar. „Token löschen **oder widerrufen**“ ist im AC `:423` zu unbestimmt gegenüber „sicher löschen/widerrufen“ der Quelle: sichere lokale Löschung und, soweit möglich/technisch vorgesehen, Widerruf samt Verhalten bei Widerrufsfehler getrennt nachweisen. Berechtigung ausschließlich Admin, Folgesync-Stop und Audit ohne Token-/Secret-Leak als testbare Kriterien aufnehmen. Kein Disconnect durch eine bloße UI-Meldung als Done. |
| **SRC-0494**, `:772–774`, Inventory `:495` | `.a/.b`, Atomic `:633–634`; Matrix `:495` → `ST-INS-04:427–435`; Task `:108` | Automatische Satztrennung zerreißt das Datum. Die Quelle hat `[x]` und dokumentiert **historisch** am 23.09.2026 zwei positive Impressionswerte 5/1 und echte CTR-Nullwerte. `ST-INS-04` ist `Planned`, das kann den vorhandenen Reach-Report als neu zu erledigen oder heutige Datenabnahme missverständlich machen. `.a/.b` zu einem Evidenz-/Statusdatensatz verbinden; `Existing / Verify` mit Datum und genauem Umfang, getrennt von den offenen Trends aus 0495/0496. Kein erneutes Implementierungsticket und kein Schluss, dass neue Reports oder Live-Stand geprüft seien. |
| **SRC-0495**, `:775–776`, Inventory `:496` | `.a`, Atomic `:635`; Matrix `:496` → `ST-INS-04:427–435`; Task `:109` | AC nennt Traffic Sources/Retention sowie API-Grenzen, verliert aber **Engagement-Zeitreihen** und den ausdrücklich sichtbaren **Datenstand**. Beide ergänzen und optional verfügbare Daten deutlich von fehlenden/API-seitig nicht unterstützten unterscheiden. `constraint-register.md:3313–3317` ergänzt: optionale Analytics-Abfragen dürfen Kern-Sync nicht blockieren; dies als Bedingung in der Story übernehmen. |
| **SRC-0496**, `:777–778`, Inventory `:497` | `.a`, Atomic `:636`; Matrix `:497` → `ST-INS-04:427–435`; Task `:110` | Fünf Zeitfenster sind im AC vorhanden. **Video-Detailseite**, Datenqualität sowie Long-Tail-/**Alterslogik** fehlen als konkret beobachtbare Ergebnisse oder werden nur im Story-Nutzen angedeutet. AC für Detailseite mit 1/7/28/90/365-Tage-Vergleich, Datenlücken, Veröffentlichungsalter und Long-Tail-Auswertung ergänzen; junge Videos nicht wie 365-Tage-Beobachtungen behandeln. |
| **SRC-0497**, `:779–780`, Inventory `:498` | `.a/.b`, Atomic `:637–638`; Matrix `:498` → `ST-INS-05:437–445`; Task `:111` | `.a` ist offene Arbeit, `.b` der **aktuelle Mangel** (Ziel/KPI nur Brief-Freitext). Das Story-AC nennt „strukturierte Planmetriken“, aber nicht eindeutige Ziel- und Ist-Werte **je Plan-Eintrag** und die genannte Datenstruktur `content_item_metrics`. Als Ergebnis eindeutige strukturierte Ziel-/Ist-Felder und ihre Zuordnung zum Eintrag prüfen; `.b` als Ausgangsstand dokumentieren, nicht als eigene neue Anforderung. Schätzung und Ist-Stunden bleiben getrennt (`ST-INS-05:443`). |
| **SRC-0498**, `:781–782`, Inventory `:499` | `.a/.b`, Atomic `:639–640`; Matrix `:499` → `ST-INS-05:437–445`; Task `:112` | `ST-INS-05:443` schützt vor fehlenden Stunden/jungen Veröffentlichungen. `.a` verlangt rechnerische Effizienz aus **tatsächlichen Stunden und veröffentlichten Ergebnissen**; konkrete Berechnungsgrundlage, Datenstand und fehlende Werte sollten beobachtbar sein. `.b` ist ein sofort geltendes Verbot: Bis belastbare Werte vorliegen, **keine Erfolgsbehauptung aus Views allein**. Als gesondertes Constraint/AC erfassen, auch wenn die Metrik-Funktion noch geplant ist. Dies entspricht `constraint-register.md:759–761` zum Nutzen pro Stunde. |
| **SRC-0499**, `:783–784`, Inventory `:500` | `.a`, Atomic `:641`; Matrix `:500` → `ST-INS-06:447–455`; Task `:113` | Die Story trifft die Grundfolge, aber „externe Analyse“ ist kein autonomer Schreibzugriff. Der wiederkehrende Workflow muss im **Cockpit dokumentiert** und für reale Wiederholung verständlich sein: Export, externe Analyse, menschlich **geprüfte** Erkenntnis, danach gezielte Übernahme in **beide** Ziele Planner und Master Context mit Quelle/Freigabe. AC `:453` nennt Herkunft/Freigabe, aber nicht die dokumentierte Cockpit-Stelle und getrennte Zielübernahmen. Ergänzen; kein automatischer Import oder Publizieren aus Analyseergebnissen ableiten. |

## Freigabe- und Migrationsgrenze

`release-decisions.md` DEC-REL-001 bis 003 ist auf SRC-0491 und auf jede spätere produktive Cockpit-/Board-Änderung anzuwenden: ausdrückliche Nutzerfreigabe **des jeweiligen Release-Umfangs**; Test, Audit oder Plan sind keine Veröffentlichungsfreigabe; lokal bearbeiten und prüfen, nie direkt live ändern. Der geschützte Dump und die späteren Live-Prüfungen aus SRC-0491 sind Betriebsanforderungen für eine **gesondert freigegebene Migration**, keine Erlaubnis, jetzt zu deployen. Die Scrum-Regel §§1, 3, 4, 6 verlangt klauselscharfe, reproduzierbare AC, korrekt getrennte historische und offene Zustände sowie bestandene Traceability vor Planersatz. Bis zur Nacharbeit bleibt `docs/ausbauplan.md` der aktive Plan.
