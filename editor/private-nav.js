(()=>{const esc=v=>String(v||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const current=location.pathname;const item=(href,icon,label)=>`<a href="${href}"${current===href?' aria-current="page"':''}><span aria-hidden="true">${icon}</span><b>${label}</b></a>`;async function start(){let session;try{const res=await fetch('/api/session');if(!res.ok)return;session=await res.json();}catch{return}document.body.classList.add('private-app-shell');if(document.querySelector('.private-nav'))return;const adminUsers=session.role==='admin'?item('/benutzerverwaltung','♙','Benutzerverwaltung'):'';const nav=document.createElement('aside');nav.className='private-nav';nav.setAttribute('aria-label','Privater Bereich');nav.innerHTML=`<div class="private-nav-top"><a class="private-brand" href="/">VANVENTURE <small>PRIVATER BEREICH</small></a><button class="private-nav-toggle" type="button" aria-expanded="true" aria-controls="shared-private-navigation"><span class="nav-toggle-icon" aria-hidden="true"></span><span class="visually-hidden">Navigation verkleinern</span></button></div><div class="account-summary"><p class="eyebrow">EINGELOGGT ALS</p><strong>${esc(session.displayName||session.name)}</strong></div><button class="sidebar-logout" type="button">Abmelden</button><nav id="shared-private-navigation">${item('/privat','⌂','Übersicht')}${item('/privat#profil','⚙','Konto & Einstellungen')}${item('/redaktion','✎','Redaktion')}${item('/cockpit','◫','Cockpit')}${adminUsers}</nav>`;document.body.prepend(nav);const toggle=nav.querySelector('.private-nav-toggle');toggle.addEventListener('click',()=>{if(matchMedia('(max-width: 760px)').matches){nav.classList.toggle('nav-expanded');toggle.setAttribute('aria-expanded',String(nav.classList.contains('nav-expanded')));return}const collapsed=document.body.classList.toggle('private-nav-collapsed');toggle.setAttribute('aria-expanded',String(!collapsed));});nav.querySelector('.sidebar-logout').addEventListener('click',async()=>{try{await fetch('/api/logout',{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':session.csrf},body:'{}'});}finally{location.assign('/')}});}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',start):start();})();
